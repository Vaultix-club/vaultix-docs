# Repository Map

The public Vaultix release is split across three repos:

1. `vaultix-web` - public site and app shell.
2. `vaultix-data` - reference market ingestion and output snapshots.
3. `vaultix-docs` - public history, architecture, privacy, and launch notes.

This layout is intentionally different from the older Nomexis repo pattern.
