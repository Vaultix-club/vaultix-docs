# Testing And Optimization

- Validate the static site renders without build tooling.
- Validate the data contract remains compatible with the app shell.
- Compare the dashboard's fallback assets with the synced JSON payload before release.
- Confirm wallet connection copy matches the demo-state behavior.
